\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage[ruled,vlined]{algorithm2e}
\usepackage{booktabs}
\usepackage{multirow}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\setlength{\parindent}{0pt}
\usepackage{caption} 
\setlength{\textfloatsep}{10pt plus 1.0pt minus 2.0pt}
\setlength{\floatsep}{8pt plus 1.0pt minus 2.0pt}

\begin{document}

\title{Distributed Multi-GPU Sparse Matrix-Vector Multiplication (SpMV) via 1D Cyclic Partitioning and GPU-Aware Communication}

\author{\IEEEauthorblockN{Michael Bernasconi}
\textit{Student ID: 267681}\\
\textit{michael.bernasconi@studenti.unitn.it} \\
\url{https://github.com/Michael-Bernasconi/Sparse-Matrix-Vector-Multiplication/tree/Deliverable2}
\IEEEauthorblockA{\textit{Department of Information Engineering and Computer Science} \\
\textit{University of Trento}\\
}
}

\maketitle

\begin{abstract}
Distributed Sparse Matrix-Vector multiplication (SpMV) represents a vital bottleneck in massive-scale numerical computing, where efficiency is heavily constrained by structural irregularity, network latency, and load imbalance. This study extends single-node architectures to a multi-GPU environment by evolving a continuous block partition layout into a 1D cyclic distribution ($owner(i) = i \pmod P$). By completely removing high-overhead global collective broadcast procedures ($MPI\_Bcast$), a custom point-to-point sparse communication overlay is implemented. This layout tracks, isolates, and exchanges exclusively the non-local active components of the multiplier vector $x$ (Ghost Entries) through non-blocking peer-to-peer routines. Furthermore, intermediate host-staged buffer paths are eliminated by refactoring the pipeline with GPU-Aware MPI paradigms operating directly on device memories. Performance evaluations conducted on the University of Trento cluster (Ampere A30 GPUs) demonstrate that while 1D cyclic mapping perfectly balances uniform data distributions (e.g., ASIC structures), highly skewed power-law structures remain strictly bound by communication overhead, providing clear experimental insights regarding the intersection of data topology, interconnect bandwidth, and multi-GPU scalability.
\end{abstract}

\begin{IEEEkeywords}
SpMV, Multi-GPU CUDA, 1D Cyclic Partitioning, Ghost Entries Exchange, GPU-Aware MPI, Load Balancing, Performance Interconnect Bound, Strong Scaling, Weak Scaling.
\end{IEEEkeywords}

\section{Introduction}
Sparse Matrix-Vector multiplication ($y = A \times x$) is the underlying backbone of modern scientific applications, differential equation solvers, and graph analytics. Evolving single-node GPU kernels to distributed architectures introduces severe scalability roadblocks caused by non-contiguous memory layouts and network bottlenecks. 

While the initial project implementation focused on a standard 1D continuous block-row slicing scheme, real-world matrices from the SuiteSparse collection exhibit non-uniform sparsity patterns that result in major computational idle times (load imbalance) and excessive memory overhead due to redundant vector replication. 

This deliverable implements a 1D cyclic row-distribution framework designed to optimize parallel workload mapping across multiple hardware devices. By rewriting the communication infrastructure around localized peer-to-peer sparse exchanges, the system eliminates collective broadcast overheads. This report details the core design principles, structural transformations, and benchmarking outcomes of the distributed SpMV execution campaign across strong and weak scaling configurations.

\section{Parallel Design Methodology}

\subsection{Evolution via Foster's Design Strategy}
To effectively transition single-device execution paths into an enterprise-grade multi-GPU paradigm, the application architecture was evolved adhering strictly to Foster's four-stage design methodology:

\begin{itemize}
    \item \textbf{Partitioning:} Fine-grained domain decomposition is performed on the row primitives of matrix $A$. The assignment of global row indices to target cluster ranks follows a precise modular distribution scheme, formally defined as:
    \begin{equation}
        owner(i) = i \pmod{\text{size}}
    \end{equation}
    where $i$ represents the global row index and $\text{size}$ corresponds to the number of active MPI ranks (GPUs).
    \item \textbf{Communication:} Rather than distributing the entire multiplier vector $x$ via global collectives, a specialized discovery process identifies non-local indices requested by local non-zero elements ($col\_idx$). These indices represent the matrix \textit{Ghost Entries}, which are captured in isolated communication descriptors.
    \item \textbf{Agglomeration:} Rows assigned to a single executor are agglomerated into an independent, compact local sparse matrix representation (local CSR or COO layouts), converting the sparse structure into relative coordinate systems to ensure zero index-translation overhead during raw kernel execution.
    \item \textbf{Mapping:} Each MPI process is pinned to a dedicated hardware device ($NVIDIA\ A30\ GPU$), executing localized data processing routines natively within isolated CUDA streams.
\end{itemize}

\subsection{GPU-Aware Point-to-Point Communication Overlay}
\label{sec:gpu_aware_comm}
To fulfill strict efficiency and high-throughput requirements, intermediate host-side staging operations are entirely bypassed. Standard multi-GPU implementations pull device results to host memory buffers via high-latency \texttt{cudaMemcpy} transactions prior to invoking MPI routines. 

This architecture leverages a \textbf{GPU-Aware MPI} runtime. Because the underlying network stack safely decodes CUDA virtual memory spaces, device pointers are supplied directly to non-blocking point-to-point primitives (\texttt{MPI\_Irecv} and \texttt{MPI\_Isend}), followed by an explicit \texttt{MPI\_Waitall} barrier. The trade-offs between redundant numerical computations and communication framework penalties have been widely investigated within dense multi-GPU clusters to elevate iterative execution behaviors~\cite{gao2024optimization}.

\begin{algorithm}[htbp]
\footnotesize
\DontPrintSemicolon
\SetAlgoLined
\KwIn{Local Sparse Matrix ($A_{loc}$), Managed Vector Segment ($x_{loc}$)}
\KwOut{Fully Synchronized Interleaved Output Vector ($y_{global}$)}
Scan $col\_idx$ to populate Ghost Receive and Send Descriptors\;
Allocate Device Buffers \texttt{d\_ghost\_recv} and \texttt{d\_ghost\_send}\;
\BlankLine
\tcp{1. Pack local values requested by others}
\texttt{pack\_ghost\_kernel<<<(...)>>>(...)}\;
\BlankLine
\tcp{2. GPU-Aware Peer-to-Peer Non-blocking Exchange}
\texttt{MPI\_Irecv(d\_ghost\_recv, ..., src\_rank, ..., \&reqs[0])}\;
\texttt{MPI\_Isend(d\_ghost\_send, ..., dest\_rank, ..., \&reqs[1])}\;
\texttt{MPI\_Waitall(2, reqs, MPI\_STATUSES\_IGNORE)}\;
\BlankLine
Start \texttt{cudaEvent\_t} Computational Timers\;
Launch Selected CUDA SpMV Kernel (CSR, COO, or Vector)\;
Stop \texttt{cudaEvent\_t} Computational Timers\;
\BlankLine
\texttt{MPI\_Gatherv(d\_local\_y, ..., d\_interleaved\_y, ..., Root)}\;
\If{Rank == 0}{
    De-interleave \texttt{d\_interleaved\_y} into linear vector $y_{global}$\;
}
\caption{Distributed GPU-Aware SpMV Pipeline}
\end{algorithm}

\section{Experimental Setup \& Dataset}

\subsection{Hardware Infrastructure}
All execution cycles were evaluated on the \texttt{edu01} compute node (\texttt{edu-short} partition) of the University of Trento cluster. The exact hardware specifications are detailed in Table \ref{tab:hardware}. All configurations utilized CUDA v12.3.2 and OpenMPI v4.1.5 with native CUDA-Aware support enabled.

\begin{table}[htbp]
\caption{Target Hardware Specifications (UniTN Cluster)}
\label{tab:hardware}
\centering
\resizebox{\columnwidth}{!}{
\begin{tabular}{rll}
\toprule
\textbf{Component} & \textbf{Host (CPU)} & \textbf{Device (GPU)} \\
\midrule
\textbf{Model} & Intel(R) Xeon(R) Silver 4309Y & NVIDIA A30 \\
\textbf{Architecture} & Ice Lake (x86\_64) & Ampere (Compute Cap. 8.0) \\
\textbf{Cores / SMs} & 16 Cores / 32 Threads (2 Sockets) & 56 SMs (3584 CUDA Cores) \\
\textbf{Frequency} & 2.80 GHz (Base) & 1.44 GHz (Boost) \\
\textbf{FP32 Perf.} & 1.84 TFLOPS & 10.3 TFLOPS \\
\textbf{Memory BW} & 102.4 GB/s & 933.1 GB/s (HBM2) \\
\textbf{Global Mem.} & N/A & 24 GB \\
\textbf{L2 Cache} & 20 MiB & 24 MiB \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{Dataset Optimization Strategy}
To guarantee a robust performance profile covering both Strong and Weak Scaling benchmarks, the experimental matrix framework is divided into two categories: real-world structures for Strong Scaling, and algorithmically generated structures for Weak Scaling.

Compared to the previous single-GPU experimental campaign, the target selection of sparse datasets has been intentionally modified and optimized. This adaptation was required since several larger matrices tested in the single-node setup presented structural scaling blockages and introduced high memory allocation problems or outright execution failures when distributed across the current multi-GPU cluster overlay. Thus, the workload selection has been revised to preserve evaluation stability while properly maintaining topological variance and uneven sparsity domains.

\subsubsection{Strong Scaling Dataset}
The baseline dataset comprises six distinct real-world sparse structures from the SuiteSparse Matrix Collection, carefully selected to evaluate the algorithms against different structural domains.

\begin{table}[htbp]
\caption{Real Datasets (Strong Scaling Configurations)}
\label{tab:dataset}
\centering
\resizebox{\columnwidth}{!}{
\begin{tabular}{rllrrrc}
\toprule
\textbf{ID} & \textbf{Matrix Name} & \textbf{Rows ($M$)} & \textbf{NNZ} & \textbf{Avg. NNZ} & \textbf{Dev. Std.} & \textbf{Structural Domain} \\
\midrule
1420 & ASIC\_680ks & 682,712 & 1,693,767 & 2.48 & 4.16 & Circuit (Sparse / Irregular) \\
1297 & boyd2 & 466,316 & 1,500,397 & 3.22 & 194.91 & Convex Opt. (Severe Spikes) \\
1881 & Rucci1 & 1,977,885 & 7,791,168 & 3.94 & 0.34 & Math Matrix (Rectangular) \\
2379 & webbase-1M & 1,000,005 & 3,105,536 & 3.11 & 25.35 & Web Graph (Power-Law Hubs) \\
1419 & ASIC\_320ks & 321,671 & 1,316,085 & 4.09 & 3.80 & Circuit (Medium Regular) \\
1513 & patents\_main & 240,547 & 560,943 & 2.33 & 1.94 & Citation Graph (Ultra-Light) \\
39   & bcsstk17 & 10,974 & 428,650 & 39.06 & 8.40 & Stiffness Matrix (High Density) \\
1269 & m\_t1 & 185,000 & 9,753,570 & 52.72 & 1.18 & Least Squares (Uniform Heavy) \\
38   & bcsstk16 & 4,884 & 290,378 & 59.45 & 19.23 & Stiffness Matrix (Small Dense) \\
1416 & ASIC\_100ks & 99,190 & 578,890 & 5.84 & 13.91 & Circuit (Small Regular Base) \\
\bottomrule
\end{tabular}
}
\end{table}

\subsubsection{Weak Scaling Dataset (Synthetic Generation)}
To properly evaluate Weak Scaling, the problem size must grow proportionally to the number of computational resources ($N$, $2N$, $4N$). We developed a custom Python generator creating two distinct synthetic topologies with a constant $D_{avg} \approx 3.0$:
\begin{itemize}
    \item \textbf{ASIC-like (\texttt{synth\_asic}):} Highly sparse but structurally uniform. Ensures perfect 1D cyclic load balancing while maintaining randomized non-zero column placements to simulate cache-miss irregularities.
    \item \textbf{BOYD2-like (\texttt{synth\_boyd2}):} Designed for extreme load imbalance. Exactly 2\% of the rows absorb 75\% of the total Non-Zeros, mimicking power-law hub topologies and severely punishing 1D distribution schemes.
\end{itemize}

\begin{table}[htbp]
\caption{Synthetic Datasets (Weak Scaling Configurations)}
\label{tab:dataset-syth}
\centering
\resizebox{\columnwidth}{!}{
\begin{tabular}{llrrrl}
\toprule
\textbf{Target GPUs} & \textbf{Matrix Name} & \textbf{Rows ($M$)} & \textbf{Total NNZ} & \textbf{Avg. D} & \textbf{Structural Profile} \\
\midrule
1 GPU & synth\_asic\_1gpu & 200,000 & $\approx$ 600,000 & 3.0 & Balanced / Uniform \\
1 GPU & synth\_boyd2\_1gpu & 200,000 & 600,000 & 3.0 & Imbalanced / Hubs \\
2 GPUs & synth\_asic\_2gpu & 400,000 & $\approx$ 1,200,000 & 3.0 & Balanced / Uniform \\
2 GPUs & synth\_boyd2\_2gpu & 400,000 & 1,200,000 & 3.0 & Imbalanced / Hubs \\
4 GPUs & synth\_asic\_4gpu & 800,000 & $\approx$ 2,400,000 & 3.0 & Balanced / Uniform \\
4 GPUs & synth\_boyd2\_4gpu & 800,000 & 2,400,000 & 3.0 & Imbalanced / Hubs \\
\bottomrule
\end{tabular}
}
\end{table}

\section{Performance Evaluation}
Execution statistics were gathered across 1, 2, and 4 GPU configurations. Custom execution frameworks (Distributed CSR, COO, CSR-Vector, and cuSPARSE wrappers) were benchmarked against the official multi-GPU SpMV baseline (\texttt{prof-SpMV-baseline}). 

\subsection{Execution Time and Computational Throughput}
As depicted in Figure \ref{fig:time} and Figure \ref{fig:gflops}, the vendor-optimized \texttt{cuSparse} framework retains dominance in raw execution speed and GFLOPS per SpMV, particularly on large matrices such as \texttt{Rucci1} and \texttt{webbase-1M}. However, among the custom implementations, the \texttt{CSR-Vector} kernel exhibits an impressive computational throughput (reaching peaks near 300 GFLOPS locally), effectively managing the warp-divergence typically associated with standard CSR implementations. 

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../../results/plots/strong_individual_time.pdf}
    \caption{Execution Time (s) per SpMV on Real Matrices (Log Scale).}
    \label{fig:time}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../../results/plots/strong_individual_gflops.pdf}
    \caption{Computational Throughput (GFLOPS) per Real Matrix.}
    \label{fig:gflops}
\end{figure}

\subsection{Strong Scaling: Speedup and Efficiency}
As visualized across the real datasets in Figures \ref{fig:speedup} and \ref{fig:efficiency}, the Strong Scaling capabilities display clear behavioral trends among the different configurations. While adding additional GPUs introduces non-trivial network overhead, the \texttt{CSR-Vector} approach scales the best among custom solutions, yielding an average speedup of $2.75\times$ on 4 GPUs with an efficiency of roughly $68.9\%$. Conversely, the \texttt{baseline} implementation completely fails to scale on our setup ($0.98\times$ on 4 GPUs with an efficiency drop down to $24\%$), largely due to the severe unoptimized \texttt{MPI\_Bcast} overheads which our Ghost-Exchange implementation actively bypasses. 

The individual matrix speedup and efficiency curves are detailed in Figures \ref{fig:speedup} and \ref{fig:efficiency}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../../results/plots/strong_individual_speedup.pdf}
    \caption{Strong Scaling Speedup Factor per Real Matrix.}
    \label{fig:speedup}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../../results/plots/strong_individual_efficiency.pdf}
    \caption{Parallel Efficiency per Real Matrix (1, 2, and 4 GPUs).}
    \label{fig:efficiency}
\end{figure}

\subsection{Communication vs Computation Breakdown}
A crucial aspect of distributed GPU programming is understanding the cost of network synchronization. Figure \ref{fig:comm_breakdown} isolates the structural impact of MPI interactions, revealing the massive relative cost of the \texttt{MPI} Ghost Exchange. Because the local SpMV CUDA kernels execute extremely fast (often in the order of tens of microseconds), the time required to pack communication buffers, marshal data over the PCIe/NVLink interconnect, and wait for peer-to-peer non-blocking completion effectively dwarfs the pure computation time. 

An analytical breakdown of the data shows that on 4 GPUs, the custom custom-tailored implementations are completely communication-bound: for instance, the \texttt{COO} kernel suffers from a Ghost Exchange overhead that is approximately $48\times$ higher than its pure compute time. Similarly, \texttt{CSR} and \texttt{CSR-Vector} register communication overhead indicators that are respectively $35\times$ and $39\times$ greater than the localized execution cost. Even the vendor-optimized \texttt{cuSparse} pipeline is heavily penalized, with network-related wait times scaling up to $18\times$ higher than the pure numerical computation phase. This behavior confirms that increasing the GPU count beyond a certain threshold yields diminishing parallel returns, as the runtime becomes strictly limited by interconnect synchronization latencies.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../../results/plots/strong_comm_comp_breakdown.pdf}
    \caption{Execution Cost Breakdown: Pure CUDA Compute vs. MPI Ghost Communication Overhead.}
    \label{fig:comm_breakdown}
\end{figure}

\subsection{Weak Scaling Profiling}
To isolate the structural impact on multi-node balancing, Weak Scaling was evaluated using synthetic datasets (Fig. \ref{fig:weak_scaling}). The custom implementations maintain excellent scaling efficiency on the \texttt{synth\_asic} family (where 1D cyclic distribution ensures equal load), dropping only slightly below $0.90$ efficiency on 4 GPUs. Conversely, the heavily imbalanced \texttt{synth\_boyd2} datasets induce severe efficiency collapses ($\approx 0.25$ on 4 GPUs), as specific ranks are flooded by dense rows while peer ranks remain idle.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../../results/plots/weak_individual_synthetic.pdf}
    \caption{Weak Scaling Efficiency and Speedup on Synthetic Workloads.}
    \label{fig:weak_scaling}
\end{figure}

\section{Discussion}

\subsection{When 1D Partitioning Works Well}
1D cyclic partitioning delivers excellent execution scaling when applied to matrices featuring low row-density standard deviations ($\sigma \le 4.16$), such as \texttt{Rucci1}, \texttt{m\_t1}, and the synthetic \texttt{synth\_asic} families. Because rows are distributed in an interleaved manner, localized variations in non-zero density are filtered out. This ensures that each independent GPU node receives an identical computational load.

\subsection{When 2D Partitioning is Advantageous}
\label{sec:discussion_2d}
1D cyclic partitioning fails when processing highly skewed power-law graphs or extreme optimization structures like \texttt{boyd2} ($\sigma = 194.91$) and our \texttt{synth\_boyd2}. These datasets contain hyper-dense rows that act as hubs. Because a 1D allocation scheme assigns an entire row indivisibly to a single executor, the GPU that receives the hyper-dense row becomes a critical bottleneck. In this scenario, a 2D partitioning strategy is highly advantageous to break up highly connected hubs across multiple executors. As structurally argued in literature, advanced two-dimensional graph layouts and edge partitioning provide significant scaling improvements over 1D strategies when mapping scale-free networks onto thousands of cores~\cite{boman2013scalable}.

\subsection{When the Implementation is Bound by the Interconnect}
As shown in the cost breakdown (Fig. \ref{fig:comm_breakdown}), the implementation becomes strictly bound by the interconnect network when scaling to 4 GPUs. The time required by \texttt{MPI\_Isend} and \texttt{MPI\_Irecv} routines to exchange non-local elements over the PCIe/network interface easily exceeds the pure execution time of the CUDA sparse multiplication kernel. Under these conditions, scaling to 4 GPUs yields decreasing performance returns, as the communication overhead dominates the total Time To Solution.

\subsection{Largest Manageable Matrix Size}
With the current memory layout, the maximum manageable matrix size is bounded primarily by the available memory on a single GPU node (NVIDIA A30 24GB). By replacing the global vector broadcast mechanism with a localized Ghost Entry exchange, this implementation avoids duplicating the full multi-million-element multiplier vector $x$ on every device, allowing the cluster to easily handle large datasets exceeding 50 million non-zero elements per node context.

\section{Conclusion}
This study expanded custom sparse matrix execution paths into a highly optimized distributed multi-GPU paradigm. Implementing a 1D cyclic distribution pattern effectively minimizes load imbalances for uniform structural topologies. Transitioning to a sparse peer-to-peer point-to-point network model built on GPU-Aware MPI concepts significantly reduces communication overheads compared to standard collective broadcasts. However, experimental results underscore the critical importance of matching data topology with appropriate parallel distribution models, as highly skewed matrices remain severely limited by interconnect performance and load imbalance under 1D constraints.

\begin{thebibliography}{00}
\bibitem{boman2013scalable} E. G. Boman, K. D. Devine, and S. Rajamanickam, ``Scalable Matrix Computations on Large Scale-Free Graphs Using 2D Graph Partitioning,'' in \textit{Proceedings of the ACM Symposium on Parallelism in Algorithms and Architectures (SPAA)}, 2013.
\bibitem{gao2024optimization} J. Gao, W. Ji, and Y. Wang, ``Optimization of Large-Scale Sparse Matrix-Vector Multiplication on Multi-GPU Systems,'' \textit{ACM Transactions on Architecture and Code Optimization (TACO)}, vol. 21, no. 4, art. 69, 2024.
\end{thebibliography}

\end{document}